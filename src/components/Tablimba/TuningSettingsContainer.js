import styled from "styled-components"

const TuningSettingsContainer = styled.div`
  position:absolute;
  right: -35px;
  box-shadow: none;
  
  div {
      background-color: transparent;
      box-shadow: none;
      border-radius: 50%;
  }  
`

export default TuningSettingsContainer